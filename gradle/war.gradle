apply plugin: "war"

bootWar {
    mainClassName = "com.greenapi.cabinet.CabinetApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
